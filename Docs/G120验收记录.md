# G120 控制端子验收记录

日期：2026-09-22。环境：Unity 2022.3.62f3c1，ElectricalTraining 场景。

## 实现范围

- 原有点位保留，新增上排12点、下排14点，标准控制端子共32个；上下端子排总端口数为71、62。
- 使用独立参考端的控制信号求解，覆盖供电、分组DI、宏控制、DO、双AI/AO、PTC及直流测量。
- 视角/仿真模式可打开滚动属性，原BOP入口保留；默认宏1，其他宏仍受P0010/P0015规则控制。
- 新端点使用稳定ID，沿用接线、撤销、存档流程；测试注入与运行状态不写入接线文件。

## 已完成验证

编辑模式全量回归：268项通过、0失败。报告：`Build/Reports/g120-final-edit-tests.xml`。

覆盖端子目录和旧别名、独立AI参考端、COM奇偶分组、宏1正反转与固定转速叠加、外部控制供电与失电、反向回馈、10V/24V测量、DO继电器触点、DO1高侧/低侧负载极性、双AI输入与标定、AO回接与电流分流、PTC异常锁存与复位、三线制同时采样、零时间求解不推进斜坡/MOP。

两个AI均补充小数标定回归，避免不同标定点被参数显示步长四舍五入为相同值。PTC编号按官方资料核对：过热F07011/A07910，传感器测试异常F07016/A07015；本仿真实现即时诊断，不模拟真实器件报警延时。

## 场景验收

最终场景汇总回归：81项中78项通过、3项既有失败。本次新增的5项G120场景测试全部通过。报告：`Build/Reports/g120-final-play-tests.xml`。

已单独通过G120场景测试：新增点世界坐标与真实模块对齐、点位投影误差不超过2px、每个新增点可由射线直接拾取、实际宏1接线、G120本体及上下端子区域在视角/仿真模式下的真实点击入口、属性显示、保存/加载与撤销/恢复。两个分辨率的属性窗口边界和滚动末尾可达性均通过。扩展后的分区备注按标准端子锚点统计，排除同一物理点的辅助别名。

大范围回归发现以下3项既有失败。已临时恢复到本次修改前的HEAD版本单独复跑，三项均以相同原因失败；之后逐字节恢复了全部功能代码。对照报告：`Build/Reports/g120-baseline-existing-tests.xml`。

| 既有测试 | 对照结果 |
| --- | --- |
| `CabinetBrandingCoversBothOriginalHeaderLogos` | 柜体旧WCK标识对象不存在 |
| `DeviceBodyConnectionPointsAppearOnlyInFaultView` | 旧断言不包含已有QFFRONT3本体接点 |
| `LineTypeShowsTheCorrectConnectionPointGroups` | 旧接点分组的可见性断言失败 |

本次未修改上述柜体标识或调整这些旧断言来隐藏失败。

截图位于 `Build/Reports/g120-terminals-1920.png`、`g120-terminals-1280.png`、`g120-properties-1920.png`、`g120-properties-1280.png` 及相应 `properties-bottom` 图片。

场景测试检查真实模块偏移误差小于0.5mm，新增点和原参考点的位置、接线逻辑节点、绿色标记、稳定端点存档、撤销/恢复及两个分辨率下的窗口边界。

## 仿真边界

电机电流为属性中的模拟值；PTC为状态模拟。没有增加电机负载动力学、热积累、万用表电流档或安全认证功能。操作步骤见 [G120控制端子使用说明](G120控制端子.md)。
